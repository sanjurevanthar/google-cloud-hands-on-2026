# Notes & Observations

- API responses include `translatedText` and detected language.
- Detection endpoint returns ISO language codes and confidence values.
- API key authentication used instead of OAuth.
- Text is URL encoded for REST submission.
- Multiple detection inputs supported in a single request.

## Future Experiments
- Try additional language pairs
- Test neural premium models
- Compare translation quality for longer paragraphs
